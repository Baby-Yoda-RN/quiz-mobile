import React, {useEffect, useState} from 'react';
import { AppProvider } from '../../context';

export const ProfileScreen = () => {

    // Fetch user info
    // Change state to be user info
    // Render user info
    return (<></>)
};