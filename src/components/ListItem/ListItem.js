import React from 'react';
import {Item} from './ListItem.view';

import {FlatList, Text, View} from 'react-native';

//Use a map function to loop the array, setup the keys.
export const ListItem = () => {
  return <Item />;
};
