import React from 'react';
import {SafeAreaView, Text, View, useColorScheme} from 'react-native';
import {stepprogress} from './stepprogress.view';

export const StepProgress = () => {
  return (
    <StepProgress placeholder = {placeholder}/>
  );
};