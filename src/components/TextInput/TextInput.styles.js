import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textInputStyles: {
    backgroundColor: '#fff',
    borderColor: '#e5e5e5',
    borderWidth: 1,
    borderRadius: 8,
    paddingHorizontal: 10,
  },
});

