import React from 'react';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 22,
    backgroundColor: '#fff',
    borderColor: '#e5e5e5',
    borderWidth: 1,
    borderRadius: 8,
  },
  item: {
    padding: 10,
    fontSize: 18,
    height: 44,
  },
});
